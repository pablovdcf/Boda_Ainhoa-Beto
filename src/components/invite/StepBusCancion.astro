<section class="wizard-step card p-6 mt-6 hidden" data-step="cancion">
  <p class="uppercase tracking-[.24em] text-[11px] text-slate-500">Paso 4</p>
  <h2 class="font-serif text-2xl text-night mt-2">Canción y mensaje</h2>

  <div class="mt-4">
    <label for="emailInput" class="text-sm font-medium text-slate-700">Email (opcional)</label>
    <input
      id="emailInput"
      type="email"
      autocomplete="email"
      inputmode="email"
      class="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2"
      placeholder="tu@email.com"
    />
    <p class="mt-1 text-xs text-slate-500">
      Si lo dejas, podremos enviarte la invitación y un recordatorio de calendario.
    </p>
  </div>

  <div class="mt-4">
    <label for="cancionInput" class="text-sm font-medium text-slate-700">Tu canción (opcional)</label>
    <input
      id="cancionInput"
      type="text"
      class="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2"
      placeholder="Título - Artista"
    />
  </div>

  <div class="mt-4">
    <label for="notasInput" class="text-sm font-medium text-slate-700">
      Mensaje para los novios (opcional)
    </label>
    <textarea
      id="notasInput"
      rows="3"
      class="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2"
      placeholder="Si quieres añadir algo más, escríbelo aquí."
    ></textarea>
  </div>

  <p id="errorCancion" class="hidden mt-3 text-sm text-rose-600" role="alert"></p>

  <div class="mt-6 flex flex-wrap justify-between gap-2">
    <button id="btnCancionBack" type="button" class="btn btn-outline">Volver</button>
    <button id="btnCancionNext" type="button" class="btn">Siguiente</button>
  </div>
</section>
