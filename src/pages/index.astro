---
import BaseLayout from "../layouts/BaseLayout.astro";
import Button from "../components/ui/Button.astro";
import Card from "../components/ui/Card.astro";
import Badge from "../components/ui/Badge.astro";
---

<BaseLayout title="Boda · Ainhoa & Alberto" description="Landing en migración a Astro">
  <main id="main-content" class="section-wrap py-16">
    <section class="surface-card bg-hero p-8 text-white sm:p-10">
      <Badge tone="neutral" className="bg-white/20 text-white border-white/30">
        Fase 1 · Scaffold Astro
      </Badge>
      <h1 class="mt-4 text-5xl font-semibold leading-tight">Ainhoa & Alberto</h1>
      <p class="mt-3 max-w-xl text-sand">
        Base migrada a Astro + Tailwind local. En esta fase mantenemos compatibilidad de
        rutas y API legacy sin tocar la lógica funcional de GAS/JSONP.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <Button href="/invite">Ir a RSVP</Button>
        <Button href="/admin" variant="secondary">Ir a Admin</Button>
      </div>
    </section>

    <section class="mt-10 grid gap-4 md:grid-cols-3">
      <Card className="p-5">
        <h2 class="text-xl font-semibold">Rutas activas</h2>
        <p class="muted mt-2 text-sm">`/`, `/invite`, `/invite.html`, `/admin`, `/admin.html`.</p>
      </Card>
      <Card className="p-5">
        <h2 class="text-xl font-semibold">Assets legacy</h2>
        <p class="muted mt-2 text-sm">
          Manteniéndose en `public/assets/*` para evitar roturas de paths existentes.
        </p>
      </Card>
      <Card className="p-5">
        <h2 class="text-xl font-semibold">PWA</h2>
        <p class="muted mt-2 text-sm">
          Nuevo SW con cache versionado y navegación `network-first` para evitar HTML viejo.
        </p>
      </Card>
    </section>
  </main>
</BaseLayout>
